{
  "name": "calorielens",
  "private": true,
  "version": "0.0.1",
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "overview": "node scripts/overview.mjs",
    "overview:check": "pnpm overview && git diff --exit-code -- docs/OVERVIEW.md",
    "check": "biome check .",
    "check:fix": "biome check . --write",
    "format": "biome format . --write"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.6",
    "typescript": "^5.9.3"
  }
}
