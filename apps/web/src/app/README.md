# app

Routing and composition layer only.

Contains:

- route files (`page.tsx`)
- layouts
- global styles/providers

Should not contain:

- raw API transport logic
- feature business logic
- domain entity models
